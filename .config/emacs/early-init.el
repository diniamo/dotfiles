;;; -*- lexical-binding: t -*-

;; Reset by gcmh
(setq gc-cons-threshold most-positive-fixnum)

(setq inhibit-splash-screen t
      inhibit-startup-message t)
(tool-bar-mode -1)
(menu-bar-mode -1)
(scroll-bar-mode -1)
